{
   "name": "surf-forecast-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && ./node_modules/.bin/vite build",
    "start": "cd backend && node server.js",
    "install": "npm install && cd frontend && npm install && cd ../backend && npm install"
    }
  }